{
  "name": "cozy-lantern",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:mobile": "turbo run dev --filter=@cozy-lantern/mobile",
    "dev:web": "turbo run dev --filter=@cozy-lantern/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
