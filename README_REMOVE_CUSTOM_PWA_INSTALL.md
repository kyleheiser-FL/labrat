# LabRat PWA Standard Install Patch

This patch removes the custom in-app PWA install guide/modal and keeps the native browser PWA install prompt.

It preserves:
- service worker registration
- manifest support
- theme-aware manifest selection
- existing LabRat UI and functionality

Apply from the repo root with the command provided in ChatGPT.
