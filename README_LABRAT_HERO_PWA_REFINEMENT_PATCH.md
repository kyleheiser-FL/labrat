# LabRat Hero + PWA Screenshot Refinement Patch

Includes:
- Keeps the tablet Neon layout unchanged.
- Fixes Clinical tablet right-side rat artwork sizing/position.
- Fixes both phone hero layouts so the rat artwork is a background layer and does not resize the cockpit card.
- Replaces PWA install preview assets with the actual Neon/Clinical screenshots supplied by the user.
- Updates the custom install modal to show three theme-matched mobile screenshots instead of one.
- Updates both theme manifests with 3 wide + 3 narrow screenshots per theme and cache-busted URLs.

Apply from the repository root.
