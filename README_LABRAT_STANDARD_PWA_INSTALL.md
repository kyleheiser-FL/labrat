# LabRat Standard PWA Install Patch

This patch removes LabRat's custom PWA install screen behavior and removes manifest screenshots so mobile browsers use the normal compact PWA install prompt instead of the screenshot-heavy install panel.

It does not change Firebase auth, cycle data, compounds, shop logic, PWA service worker registration, or theme styling.
