# LabRat Mobile Rat Background Restore Patch

This patch makes the Daily Cockpit rat artwork visible on phone layouts for both Clinical Dark and Neon themes without changing the size of the cockpit card.

It appends a guarded CSS override to `src/index.css` and leaves the neon tablet/desktop layout untouched.
