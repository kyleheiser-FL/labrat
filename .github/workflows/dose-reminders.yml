name: Dose Reminder Cron

# Vercel Hobby plan caps crons at once/day, so we poll the reminder endpoint
# from GitHub Actions instead. /api/send-reminders uses catch-up semantics:
# it sends once a user's reminder time has passed (within a grace period) and
# a per-day pushGuard dedupes — so hourly polling delivers every reminder
# exactly once, at most ~an hour late. Hourly also stays well inside the free
# GitHub Actions minutes budget.

on:
  schedule:
    - cron: '11 * * * *' # hourly at :11 — off-peak minute; :00 runs get delayed most
  workflow_dispatch: {} # allow manual runs for testing

# Don't let a slow run stack on top of the next scheduled tick.
concurrency:
  group: dose-reminders
  cancel-in-progress: false

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger send-reminders
        run: |
          code=$(curl -sS -m 60 -o /tmp/body.txt -w "%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://labratapp.app/api/send-reminders)
          echo "HTTP $code"
          cat /tmp/body.txt || true
          echo
          if [ "$code" != "200" ]; then
            echo "::error::send-reminders returned HTTP $code"
            exit 1
          fi
